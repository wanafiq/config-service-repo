server:
  port: 8082

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://ep-damp-sea-a1g2d237-pooler.ap-southeast-1.aws.neon.tech/user-service?sslmode=require&channel_binding=require
    username: user-service
    password: npg_evlKHQV2h4to
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    com.wanafiq.userservice: DEBUG
    org.springframework.security: WARN
    org.hibernate: WARN
    com.netflix.discovery: WARN

app:
  security:
    auth-server:
      jwk-set-uri: http://localhost:8081/oauth2/jwks
